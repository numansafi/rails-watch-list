<h1><%= @list.name %></h1>

<p><%= link_to "Add a movie", new_list_bookmark_path(@list) %></p>

<ul>
  <% @bookmarks.each do |bookmark| %>
    <li><%= bookmark.movie.title %>- <%= link_to "delete", bookmark_path(bookmark), method: :delete %></li>
  <% end %>
